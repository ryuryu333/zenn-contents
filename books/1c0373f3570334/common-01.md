---
title: "この本の読み進め方"
---

# 1. この章でやること
この章では、本書の対象読者と読み進め方を整理します。

また、レイヤー別に「どの章を読めばよいか」の目安も提示します。
ユーザー環境だけ、開発環境だけ、とちいさく始めてみたい場合は参考にしてください。


# 2. 誰のための本か
本書は、すでに Homebrew などで環境構築を行っているユーザーを想定しています。

**主に Mac ユーザー を対象に解説**します。
基本的な仕組みは Linux でも共通ですが、Mac 特有の設定（nix-darwin）を含みます。

**Windows（WSL）/ Linux  ユーザーの方は、nix-darwin と書かれた箇所は読み飛ばしてください**。


# 3. 読み進め方
本書は `common` / `user` / `mac` / `dev` の4レイヤー構成です。

**最初に `common` レイヤー（共通必須）を進めてから、目的に応じて `user` / `mac` / `dev` を選択する**読み方をおすすめします。

全レイヤーを進めると Mac の環境全体を管理できるようになりますが、最初から実施するのは**学習コストが大きい**かと思います。

1. home-manager でユーザー環境（CLI ツールなど）を管理する
2. home-manager で管理できないツール（Homebrew にしかないツール）を nix-darwin で補完する
3. nix-darwin で Mac のシステム設定も管理する
4. Flakes devShells で開発環境を分離・再現する

そのため、興味がある内容から触ってみることをおすすめします。
以下にモデルケースを示します。


# 4. レイヤー別の読み進め方
## 4.1 共通レイヤー（必須）
common-01 → common-02 → common-03 → common-04 → common-05

## 4.2 ユーザー環境レイヤー
user-01 → user-02 → user-03 → user-04 → user-05

## 4.3 Mac 全体レイヤー
mac-01 → mac-02 → mac-03 → mac-04 → mac-05 → mac-06

## 4.4 開発環境レイヤー
dev-01 → dev-02 → dev-03 → dev-04


:::message
まずは **common レイヤー + 目的の1レイヤー** だけ試すのがおすすめです。

既存の管理ツール（Homebrew 等）と Nix は併用できます。
ですので、ユーザー環境だけ / 開発環境だけ Nix を試すことも可能です。

例えば、ユーザー環境だけ管理したい場合は `common` + `user`、Mac 全体を管理したい場合は `common` + `user` + `mac`、開発環境だけを整備したい場合は `common` + `dev` を選んでください。

まずは興味がある使い方で Nix に触れてみてください。

そして、Nix にハマったら徐々に Nix 管理の幅を広げていくと良いと思います。

---

**個人的には、home-manager と Flakes devShells の導入だけでも十分かもしれないと考えています**。
開発に必要なツールを Nix で一手に管理することで、ツールのバージョン管理の負荷が大きく低減できて楽です。

nix-darwin は強力ですが、最初に手を出すのは大変かもしれません。
:::
