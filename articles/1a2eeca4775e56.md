---
title: "GitHub Actions における Nix バイナリキャッシュ 3 手法の実測比較"
emoji: "🐡"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: []
published: false

---

# はじめに
GitHub Actions の CI/CD に Nix を利用する際、**バイナリキャッシュを活用することでビルド時間を短縮できます**。

キャッシュツールは複数存在しており、それぞれ特徴があります。

本記事では、3 種類の方法を「**実際のビルド時間**」で比較します。

- [cachix-action](https://github.com/cachix/cachix-action)
- [cache-nix-action](https://github.com/nix-community/cache-nix-action)
- [magic-nix-cache-action](https://github.com/DeterminateSystems/magic-nix-cache-action)

**どのツールが自身のプロジェクトに最適化判断する材料となれば幸いです**。

:::message alert
**検証条件によって適したツールは変わります**。
**あくまで私の検証ではこうだった、という参考程度にとどめてください**。
:::


# 対象読者

- GitHub Actions × Nix で CI を回しており、ビルド時間短縮を検討している方
- キャッシュを公開したくない／リポジトリ内完結で扱いたい事情がある方
- 反対に、複数ジョブ・複数リポジトリ・複数ランナー間でキャッシュを共有したい方
- 「どれを選べばよいか」の初期判断材料がほしい方

# 結論
**基本的には `cache-nix-action` を利用し、必要に応じて `cachix-action` も比較検討する、といったスタイルが良いと思いました**。

>`cache-nix-action` は GitHub Actions Cache を 数百 MB（今回検証した環境だと、それぞれ 650 MB、480 MB） 消費します。
>他の用途で Cache を利用している場合、[キャッシュ容量制限](https://docs.github.com/en/actions/reference/workflows-and-actions/dependency-caching#usage-limits-and-eviction-policy)（リポジトリごとに 10 GB）に引っかかる可能性があります。

**`magic-nix-cache-action` はビルド時間のばらつきが大きく、job 短縮効果も乏しい（API 応答や巨大なファイル分割の影響と推測）ため、非推奨**。

#### nixpkgs だけで完結する環境の場合
キャッシュツールの恩恵は小さいので、無くてもよい。
使うなら `cache-nix-action` が有用、job が約 10% 短縮された。

![marp job 時間一覧 use cache のみ](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-30_2.webp)

#### 独自パッケージを利用する環境（e.g. `importNpmLock` で Node.js パッケージを Nix で再現）場合
最有力は `cache-nix-action`、次点で `cachix-action`。
job が約 50% 短縮された。

![zenn job 時間一覧 use cache のみ](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-30_1.webp)

# 記事の流れ

- 各ツールの紹介
- 背景
- 検証方法
- 検証結果

という流れで進行します。


# 1. 背景
## 1.1 Nix のビルドとキャッシュ
**Nix のビルドは、キャッシュにより最適化されています**。
背景知識として、簡略的にビルドの流れを紹介します。

`nix build` 等のコマンドを実行した際、`flake.nix` 等を元にし、ビルドレシピ（derivation）からビルド成果物（例：`treefmt`）がハッシュとして算出されます。

```nix:flake.nix
packages.default = buildEnv {
    name = "example";
    paths = [ pkgs.treefmt ];
};
```

<!-- cspell:disable -->
```bash
/nix/store/pnl4n8xandadzdp0jlnbkq4smkldprzs-treefmt-2.3.1
```
<!-- cspell:enable -->


**`treefmt` のバイナリを構築する前に、以下の順でビルド成果物が既にあるかを確認します**。

- ローカルの Nix ストアに同一パスがあるか
- なければ、設定されたバイナリキャッシュにあるか
- どちらにもなければ、一からビルド

**デフォルトのバイナリキャッシュは cache.nixos.org です**。
**nixpkgs の多くはここにキャッシュが存在するため、初回からビルドをスキップして高速になります**。

<!-- cspell:disable -->
```bash:ビルド時のログ例
these 464 paths will be fetched (265.13 MiB download, 1391.91 MiB unpacked):
  # ...
  /nix/store/pnl4n8xandadzdp0jlnbkq4smkldprzs-treefmt-2.3.1
  # ...
copying path '/nix/store/pnl4n8xandadzdp0jlnbkq4smkldprzs-treefmt-2.3.1' from 'https://cache.nixos.org'...
# ...
```
<!-- cspell:enable -->

一方で、**独自定義のパッケージは cache.nixos.org に存在しない**ため、初回はキャッシュを活用できません。

## 1.2 ローカル / GitHub Actions でのビルドの差異
「**ローカル環境**」では、独自定義のパッケージであっても、2 回目以降はローカルの Nix ストアでのキャッシュによりビルドが高速化されます。

しかし、「**GitHub Actions**」では、実行ごとにまっさらな環境が立ち上がるため、ローカルの Nix ストアによる再利用が効きません。

つまり、**独自定義のパッケージを含むプロジェクトでは、毎回ビルドが必要となり、CI/CD の実行時間が長くなる傾向にあります**。

## 1.3 バイナリキャッシュツール
Nix には cache.nixos.org 以外の手段でビルド成果物を保存・再利用する仕組みが用意されています。

**本記事では、GitHub Actions での Nix ビルドにフォーカスして、以下のキャッシュツールを比較します**。

- cachix-action
- cache-nix-action
- magic-nix-cache-action

:::message
**cachix、attic など、ローカルでのビルドにおいても利用できるキャッシュツールがあります**。
**本記事ではスコープ外としますので、詳細は参考資料をご覧ください**。
:::

## 1.4 最適なツールはどれか
[次項](#24-各ツールのスター数)でデータを提示しますが、スター数だけで判断するなら `magic-nix-cache-action` が良いとなるでしょう。

**しかし、本当にそうでしょうか**。

「**自分の環境にとってベストなツール（＝CI/CD が高速化できるツール）はどれか**」を知りたいと思った事が本調査のきっかけです。

**`nix build` と `ジョブ全体` の時間を評価軸とし、初回ビルドと 2 回目のビルドの時間を計測して、キャッシュツールを比較します**。


#### 参考資料
https://nixos.org/manual/nixpkgs/stable/

https://zero-to-nix.com/concepts/caching/

https://github.com/zhaofengli/attic

https://docs.cachix.org/getting-started

https://zenn.dev/asa1984/books/nix-introduction/viewer/07-binary-cache

https://zenn.dev/asa1984/articles/make-a-binary-cache


# 2. 各ツールの紹介
## 2.1 cachix-action

- 外部のバイナリキャッシュサービス（Cachix）を利用
- ローカルと GitHub Actions・複数リポジトリ・複数 Job でキャッシュを共有可能
- 無料枠あり
  - ただし、キャッシュは公開される
  - プライベートは有料
- cache.nixos.org に存在しないモノのみをキャッシュする

https://app.cachix.org/

https://github.com/cachix/cachix-action

https://zenn.dev/trifolium/articles/fc89526ce652fa

## 2.2 cache-nix-action

- GitHub Actions のキャッシュを利用
- デフォルト設定では `/nix`、`~/.cache/nix`、`~root/.cache/nix` がキャッシュ対象
- GitHub Actions Cache 上では大きな 1 ファイルとして保存される
  - e.g. cache name: `nix-${{ runner.os }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}`
- Nix の環境全体をキャッシュ＆復元するイメージ（筆者の印象）

https://github.com/nix-community/cache-nix-action

## 2.3 magic-nix-cache-action

- 細かい設定無しで手軽に利用できる
  - 公式レポジトリより引用
  - `Use Magic Nix Cache, a totally free and zero-configuration binary cache for Nix on GitHub Actions.`
- キャッシュの挙動は cachix に似ているイメージ（筆者の印象）
  - cache.nixos.org に存在しないモノのみをキャッシュする
    - のはずだが...今回の検証では cache.nixos.org にあるファイルもキャッシュしていた
    - 詳細は考察に記載、解決策募集中

https://github.com/DeterminateSystems/magic-nix-cache-action

## 2.4 各ツールのスター数

![GitHub Star 数の比較](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-25.webp)

https://www.star-history.com/#cachix/cachix-action&nix-community/cache-nix-action&DeterminateSystems/magic-nix-cache-action&type=date&legend=top-left

スター数だけを見ると `magic-nix-cache-action` が一番多いです。

2025 年以降に `cache-nix-action` が伸びています。
これは、`magic-nix-cache-action` が一時的に非推奨となり、`cache-nix-action` に移行したプロジェクトがあった為と推測されます。

例を探してみると、`Hyprland` が `cache-nix-action` に移行している PR を見つけました。

>2025/6/20
Describe your PR, what does it fix/add?
After https://github.com/DeterminateSystems/magic-nix-cache-action has been deprecated, we've been running Nix Actions without a cache for /nix/store. This meant that all dependencies had to be downloaded on each action run.

https://github.com/hyprwm/Hyprland/pull/10794

https://github.com/hyprwm/Hyprland/blob/main/.github/workflows/nix.yml

補足すると、2025 年 6 月頃、GitHub Actions Cache API 仕様変更の影響で、`magic-nix-cache-action` は無料枠の利用停止を予告していました。

- End of life for the free tier of the Magic Nix Cache

https://determinate.systems/blog/magic-nix-cache-free-tier-eol/?utm_source=chatgpt.com

現在は復活しており、無料で利用できる状態です。

- Bringing back the Magic Nix Cache Action

https://determinate.systems/blog/bringing-back-magic-nix-cache-action/


# 3. 検証方法
## 3.1 ビルドする環境
検証にあたり 2 種類の Nix 環境を用意しました。

- marp
  - `pkgs.marp` の用に pkgs 登録のパッケージをのみを利用する環境
- zenn
  - `importNpmLock` を利用して node_module を Nix で再現する環境
  - 公式バイナリキャッシュが活用されにくい環境

### 3.1.1 marp - pkgs.<name> のみを利用する環境
Marp CLI を利用して Markdown からスライドを静的作成するのに利用している環境です。

`pkgs.marp-cli` の様に nixpkgs のパッケージを利用します。
そのため、ビルドでは公式バイナリキャッシュ（cache.nixos.org）を活用できます。

```nix:flake.nix
{
  description = "Marp environment";

  inputs = {
    nixpkgs.url = "github:nixos/nixpkgs/nixpkgs-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };

  outputs =
    { nixpkgs, flake-utils, ... }:
    flake-utils.lib.eachDefaultSystem (
      system:
      let
        pkgs = nixpkgs.legacyPackages.${system};
      in
      rec {
        packages = {
          marp_build = pkgs.buildEnv {
            name = "marp_build_tools";
            paths = with pkgs; [
              marp-cli
              chromium
              noto-fonts-cjk-sans
            ];
          };
          default = packages.marp_build;
        };

        devShells = {
          default = pkgs.mkShell {
            nativeBuildInputs = with pkgs; [
              packages.marp_build
              gh
            ];
          };
        };

        apps = {
          marp = {
            type = "app";
            program = "${pkgs.marp-cli}/bin/marp";
          };
          default = apps.marp;
        };
      }
  );
}
```

### 3.1.2 zenn - node_module を nix で再現する環境
Zenn CLI を利用してローカル環境で記事を作成するのに利用している環境です。

Node.js のツールを利用するために、`importNpmLock` で node_module を Nix で構築します。

https://github.com/NixOS/nixpkgs/tree/master/pkgs/build-support/node/import-npm-lock

**`importNpmLock` によるビルドは公式バイナリキャッシュ（cache.nixos.org）が効きません**。
この点が、marp 環境との明確な違いです。

```nix:flake.nix
{
  description = "Zenn CLI environment";

  inputs = {
    nixpkgs.url = "github:nixos/nixpkgs/nixpkgs-unstable";
    flake-utils.url = "github:numtide/flake-utils";
  };

  outputs =
    { nixpkgs, flake-utils, ... }:
    flake-utils.lib.eachDefaultSystem (
      system:
      let
        pkgs = nixpkgs.legacyPackages.${system};
        npmRoot = ./node-pkgs;
        nodejs = pkgs.nodejs_24;
        inherit (pkgs) importNpmLock;
        npmDeps = importNpmLock.buildNodeModules {
          inherit nodejs npmRoot;
        };
      in
      rec {
        packages = {
          zenn_tools = pkgs.buildEnv {
            name = "zenn_tools";
            paths = with pkgs; [
              treefmt
              lychee
              npmDeps
            ];
          };
          default = packages.zenn_tools;
        };

        devShells = {
          zenn = pkgs.mkShell {
            nativeBuildInputs = [
              packages.zenn_tools
              pkgs.go-task
              importNpmLock.hooks.linkNodeModulesHook
            ];
            inherit npmDeps;
          };
          node = pkgs.mkShell {
            packages = [
              nodejs
              pkgs.npm-check-updates
            ];
          };
          default = devShells.zenn;
        };
      }
    );
}
```

- 参考資料

https://zenn.dev/trifolium/articles/5b01a68b80808b

https://zenn.dev/trifolium/articles/6678b0c0fb0d27


## 3.2 ワークフロー
### 3.2.1 フローの内容について
#### ジョブ
マトリックス機能を利用して、ビルド環境 2 種 x（キャッシュツール 3 種 + ツール無し）＝ 8 パターンのジョブを定義しました。

#### トリガー
手動（`workflow_dispatch`）としました。

#### トリガー時に指定する変数
キャッシュ生成を含んだジョブの時間、キャッシュがある状態でのジョブ時間を比較したかったので、実行時  に `generate-cache` `use-cache` を選択し、目印としました。
処理に違いはなく、ジョブ名にのみ利用しています。

#### アクション
各ステップで利用しているアクションは各キャッシュツールの GitHub の README.md に記載されている利用例を模倣しました。

Nix のインストールがバラバラですが、統一はせずに公式の利用例に準拠した方法を優先しました。

#### コード

<!-- cspell:disable -->
```yml:build.yml
name: "build"

on:
  workflow_dispatch:
    inputs:
      phase:
        description: "Choose cache phase. Only used for naming the job."
        required: true
        type: choice
        options:
          - generate-cache
          - use-cache

jobs:
  build:
    strategy:
      matrix:
        cache_tool: [none, cachix-action, cache-nix-action, magic-nix-cache-action]
        target_nix_env: [marp, zenn]
    name: build_${{ matrix.target_nix_env }}_cachetool_${{ matrix.cache_tool }}_phase_${{ github.event.inputs.phase }}
    runs-on: ubuntu-latest
    concurrency:
      group: ${{ matrix.target_nix_env }}_${{ matrix.cache_tool }}_${{ github.event.inputs.phase }}_${{ github.ref }}
      cancel-in-progress: true
    defaults:
      run:
        working-directory: ${{ matrix.target_nix_env }}

    steps:
      - uses: actions/checkout@v5

      # === Install nix & setup cache tool ===
      # none
      - name: Install nix
        uses: cachix/install-nix-action@v31
        if: ${{ matrix.cache_tool}} == 'none'

      # cachix Action
      - name: Install nix
        uses: cachix/install-nix-action@v31
        if: ${{ matrix.cache_tool}} == 'cachix-action'

      - name: Setup cache tool
        uses: cachix/cachix-action@v16
        with:
          name: ryuryu333
          authToken: '${{ secrets.CACHIX_AUTH_TOKEN }}'
        if: ${{ matrix.cache_tool }} == 'cachix-action'

      # cache-nix-action
      - name: Install nix
        uses: nixbuild/nix-quick-install-action@v30
        with:
          nix_conf: |
            keep-env-derivations = true
            keep-outputs = true
        if: ${{ matrix.cache_tool}} == 'cache-nix-action'

      - name: Setup cache tool
        uses: nix-community/cache-nix-action@v6
        with:
          primary-key: nix-${{ matrix.target_nix_env }}-${{ runner.os }}-${{ hashFiles('**/*.nix', '**/flake.lock') }}
          restore-prefixes-first-match: nix-${{ matrix.target_nix_env }}-${{ runner.os }}-
          gc-max-store-size-linux: 1G
          purge: true
          purge-prefixes: nix-${{ runner.os }}-
          purge-created: 0
          purge-last-accessed: 0
          purge-primary-key: never
        if: ${{ matrix.cache_tool }} == 'cache-nix-action'

      # magic-nix-cache-action
      - name: Install nix
        uses: DeterminateSystems/nix-installer-action@v20
        if: ${{ matrix.cache_tool}} == 'magic-nix-cache-action'

      - name: Setup cache tool
        uses: DeterminateSystems/magic-nix-cache-action@v13
        if: ${{ matrix.cache_tool }} == 'magic-nix-cache-action'

      # === Build ===
      - run: nix build

      # === Run test command ===
      - run: nix develop -c zenn --version
        if: ${{ matrix.target_nix_env }} == 'zenn'

      - run: nix run .#marp -- --version
        if: ${{ matrix.target_nix_env }} == 'marp'
```
<!-- cspell:enable -->

### 3.2.2 ワークフローの実行手順

- 手動で GitHub Actions の Cache を全て削除
  - `gh cache delete --all` を実行
- cachix の Cache を全て削除
  - Caches > ryuryu333 > Settings > Danger Zone > Clear をクリック
- 手動でワークフロー `build` を実行
  - `generate-cache` を選択
- 全てのジョブが完了するまで待機
- 手動でワークフロー `build` を実行
  - `use-cache` を選択
- 全てのジョブが完了するまで待機
- 最初に戻る

以上のフローを 4 回実施しました。

## 3.3 ジョブ時間の集計
GitHub API を利用して各ジョブ・ステップのログを取得し、Python で集計・グラフ化しました。

https://docs.github.com/ja/rest?apiVersion=2022-11-28

集計対象は以下の通りです。

| run_id       | run_number | note |
|---------------|-------------|------|
| 18753771242   | 15          | 1 generate-cache   |
| 18754054171   | 16          | 1 use-cache   |
| 18777714953   | 21          | 2 generate-cache   |
| 18777913273   | 22          | 2 use-cache   |
| 18778308420   | 23          | 3 generate-cache   |
| 18778547830   | 24          | 3 use-cache   |
| 18779716921   | 27          | 4 generate-cache   |
| 18779934804   | 28          | 4 use-cache   |

https://github.com/ryuryu333/nix_cache_ci_experiments/actions/workflows/build.yml

# 4. 検証結果
## 4.1 marp - pkgs.<name> のみを利用する環境
### 4.1.1 job 時間

| キャッシュツール          | 時間（秒） | vs none  |
|---------------------------|----------:|-------:|
| none                      | 36.50      | -   |
| cachix-action             | 42.00      | 115%    |
| cache-nix-action          | 31.75      | 87%    |
| magic-nix-cache-action    | 54.00      | 148%   |

![marp job 時間一覧 use cache のみ](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-30_2.webp)

:::message
#### キャッシュツールの効果はあったか？
キャッシュツールの恩恵は少なく、むしろ、キャッシュツールの導入によるオーバーヘッドで、job 時間が長くなりました。

build の殆どが公式バイナリキャッシュを利用でき、キャッシュ対象が無いためと思われます。

例外的に、`cache-nix-action` のみ job 時間が短縮されました。
:::

:::message
#### `cache-nix-action` だけ短縮効果があったのはなぜ？
job 時間だけだと分からないので、build 工程にフォーカスして調べてみます。
:::


### 4.1.2 build 時間

| キャッシュツール          | 時間（秒） | vs none  |
|---------------------------|----------:|-------:|
| none                      | 24.00      | -   |
| cachix-action             | 25.25      | 105%    |
| cache-nix-action          | 9.25      | 39%    |
| magic-nix-cache-action    | 30.75      | 128%   |

>全て 2 回目のビルド＝キャッシュを利用した状態でのビルド時間

![marp build 時間一覧](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-27_2.webp)

::::message
#### `cache-nix-action` だけ build 時間が短いのはなぜ？
キャッシュ範囲の違いに起因すると思われます。

`cache-nix-action` は `nix/` 全体を GitHub Actions Cache に保存・復元します。

>正確には、`nix/` 以外にもキャッシュ対象があり、また、`nix/` 以下であっても対象外のファイルがあります。詳細は[公式ドキュメントの Limitations](https://github.com/nix-community/cache-nix-action/tree/v6/?tab=readme-ov-file#limitations) を確認ください。

対照的に、`cache-nix-action` 以外の条件では、パッケージごとにキャッシュをダウンロードします。

>例えば、今回の環境では 300 回以上のダウンロードが走っています。

ダウンロード処理の回数が少ないので `cache-nix-action` は早いのかもしれません。

:::details 具体的なログ・数値が見たい人向け
`cache-nix-action` を用いた job のログを確認してみます。

`Setup cache tool` にてキャッシュツールをインストールしています。
同時に、キャッシュを検索し、ヒットしたファイル（645 MB）を GitHub Actions Cache からダウンロード・解凍しています。

そして、`Run nix build` では公式バイナリキャッシュから殆どダウンロードしておらず、短時間で完了しています。

- [ログリンク build #16 marp cache-nix-action](https://github.com/ryuryu333/nix_cache_ci_experiments/actions/runs/18754054171/job/53501163592)

![marp cache-nix-action 各ステップの処理時間](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-30_3.webp)

<!-- cspell:disable -->
```bash
Run nix-community/cache-nix-action@v6
Searching for a cache with the key "nix-marp-Linux-e3c495157b5be791a707dd6fbc7bbc8f41a8d450652fb851bfd5061f2c30a2ef".
# ...
Restoring a cache with the key "nix-marp-Linux-e3c495157b5be791a707dd6fbc7bbc8f41a8d450652fb851bfd5061f2c30a2ef".
Logs produced while restoring a cache.
  Cache hit for: nix-marp-Linux-e3c495157b5be791a707dd6fbc7bbc8f41a8d450652fb851bfd5061f2c30a2ef
  Received 0 of 676245522 (0.0%), 0.0 MBs/sec
  Received 134217728 of 676245522 (19.8%), 63.0 MBs/sec
  # ...
  Cache Size: ~645 MB (676245522 B)
  /usr/bin/tar -xf /home/runner/work/_temp/13e5a837-8e9a-419b-8577-dbf9cd164781/cache.tzst -P -C /home/runner/work/nix_cache_ci_experiments/nix_cache_ci_experiments --exclude-from /home/runner/work/_temp/2fe1990c-9bc4-4e74-8887-53119591df1a/paths --use-compress-program unzstd
  Cache restored successfully
Finished restoring the cache.
# ...
```

```bash
Run nix build
copying path '/nix/store/3qb1vcqah87n5hi5pd57m7jmjn78lpkb-source' from 'https://cache.nixos.org'...
# この 2 行のみで完了
```

キャッシュツール無しの場合、`Run nix build` で 381 個のファイル（437.39 MiB）をダウンロードしています。

- [ログリンク build #16 marp none cachetool](https://github.com/ryuryu333/nix_cache_ci_experiments/actions/runs/18754054171/job/53501163455)

![キャッシュツール無し 各ステップの処理時間](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-31.webp)

```bash
Run nix build
# ...
this derivation will be built:
  /nix/store/xa5vqc5v7ingk0x556qiy38dd18fvbz0-marp_build_tools.drv
# ...
these 381 paths will be fetched (437.39 MiB download, 1937.79 MiB unpacked):
# ...
copying path '/nix/store/1dw13nzqf9v8w7jcy205hkp7s7ggi0cc-builder.pl' from 'https://cache.nixos.org'...
# ...
```
<!-- cspell:enable -->
:::
::::

:::message
#### どのキャッシュツールが marp 環境においてベストか？
`cache-nix-action` がベスト。
それ以外のツールはキャッシュがある状態であっても、ツール無しの方が早い。
:::

### 4.1.3 job 時間（キャッシュ生成時 vs キャッシュ利用時）

| キャッシュツール          | キャッシュ生成時<br>job 時間（秒） | vs none  | キャッシュ利用時<br>job 時間（秒） | vs none  |
|---------------------------|----------:|-------:|----------:|-------:|
| none                      | 36.50      | -   | 36.50      | -   |
| cachix-action             | 42.75      | 117%    | 42.00      | 115%   |
| cache-nix-action          | 45.50      | 125%    | 31.75      | 87%   |
| magic-nix-cache-action    | 428.50      | 1174%   | 54.00      | 148%  |

![marp job 時間一覧](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-27.webp)

:::message
#### キャッシュ生成速度に差はあるのか？
`magic-nix-cache-action` が突出してキャッシュ生成時間が長くなりました。

他のツールにおいては、大きな差異は確認されませんでした。
:::

:::message
#### `magic-nix-cache-action` だけキャッシュ生成時間が長いのはなぜ？
公式バイナリキャッシュがあるパッケージであってもキャッシュし、かつ、パッケージごとにキャッシュしているのが原因だと思われます。

- [ログリンク build #15 marp magic-nix-cache-action](https://github.com/ryuryu333/nix_cache_ci_experiments/actions/runs/18754054171/job/53501163592)

![build #15 marp magic-nix-cache-action のログ](/images/1a2eeca4775e56/1a2eeca4775e56-2025-11-1.webp)

```bash
# magic-nix-cache-action の Post action
Post job cleanup.
/usr/bin/sudo mkdir -p /var/lib/determinate
  2025-10-23T15:36:51.364469Z  INFO magic_nix_cache::api: Workflow finished
    at magic-nix-cache/src/api.rs:58
# ...
  2025-10-23T15:37:36.342238Z  INFO magic_nix_cache::gha: Uploaded '/nix/store/5b625p55ssjcayg8wf31f736d3l2r6s9-marp-cli-4.2.3' to the GitHub Action Cache
    at magic-nix-cache/src/gha.rs:214
# ...
# 上記アップロード処理が 1000 行ほど（約 300 ファイル）続く
```

本来は marp-cli 等の公式バイナリキャッシュがあるパッケージはキャッシュしないはずです。
[Action Options](https://github.com/DeterminateSystems/magic-nix-cache-action) には以下の様に記述されています。

>Parameter: upstream-cache
>Description: Your preferred upstream cache. Store paths in this store aren't cached in GitHub Actions' cache.
>Default: https://cache.nixos.org


しかし、実際にはキャッシュが作成されていました。
アップロード処理数が多い為、job 時間が長くなっていると思われます。
:::

:::message
#### `cachix-action` と `cache-nix-action` のキャッシュはどうなっている？

##### cachix-action
パッケージごとにアップロード処理を実行、公式バイナリキャッシュがある場合はスキップしています。

- [ログリンク build #15 marp cachix-action](https://github.com/ryuryu333/nix_cache_ci_experiments/actions/runs/18753771242/job/53500180884)

```bash
Post job cleanup.
Cachix: push
  [2025-10-23 15:36:14][Info] Starting Cachix Daemon
# ...
# 大半のパッケージはスキップ
  [2025-10-23 15:36:44][Info] Skipping /nix/store/5b625p55ssjcayg8wf31f736d3l2r6s9-marp-cli-4.2.3
# ...
# marp_build_tools のみキャッシュ
  [2025-10-23 15:36:44][Info][retry:0] Pushing /nix/store/mdm9ya8550c5d40b5a81lcvyfyzrav5b-marp_build_tools
```

##### cache-nix-action
Nix Store を整理（ガベージコレクション）した後、tar で圧縮・保存しています。

- [ログリンク build #15 marp cache-nix-action](https://github.com/ryuryu333/nix_cache_ci_experiments/actions/runs/18753771242/job/53500180949)

<!-- cspell:disable -->
```bash
Post job cleanup.
Trying to save a new cache with the key "nix-marp-Linux-e3c495157b5be791a707dd6fbc7bbc8f41a8d450652fb851bfd5061f2c30a2ef".
# ...
Searching for a cache with the key "nix-marp-Linux-e3c495157b5be791a707dd6fbc7bbc8f41a8d450652fb851bfd5061f2c30a2ef".
Found no cache with this key.
Removing useless files.
# ...
Current store size in bytes: 2336114800.
# ...
Collecting garbage.
# ...
Current store size in bytes: 2144102448.
Saving a new cache with the key "nix-marp-Linux-e3c495157b5be791a707dd6fbc7bbc8f41a8d450652fb851bfd5061f2c30a2ef".
/usr/bin/tar --posix -cf cache.tzst --exclude cache.tzst -P -C /home/runner/work/nix_cache_ci_experiments/nix_cache_ci_experiments --exclude-from /home/runner/work/_temp/50b1f790-fb67-4a3b-9320-5a5d5b707e7c/paths --files-from manifest.txt --use-compress-program zstdmt
# ...
Sent 676245522 of 676245522 (100.0%), 196.3 MBs/sec
# ...
```
<!-- cspell:enable -->
:::


## 4.2 zenn - node_module を nix で再現する環境
### 4.2.1 job 時間

| キャッシュツール          | 時間（秒） | vs none  |
|---------------------------|----------:|-------:|
| none                      | 66.25      | -   |
| cachix-action             | 40.50      | 61%    |
| cache-nix-action          | 29.75      | 45%    |
| magic-nix-cache-action    | 66.50      | 100%   |

![zenn job 時間一覧 use cache のみ](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-30_1.webp)


:::message
#### キャッシュツールの効果はあったか？
全てのツールで job 時間が短縮された。

ツール間に差があり、`cache-nix-action` が一番早かった。

`importNpmLock` による node_modules のビルド生成物は cache.nixos.org に存在せず、ローカルでのビルドが必要となります。
そのため、cachix や GitHub Actions Cache にキャッシュすることでビルドが短縮できていると思われます。
:::

:::message
#### marp 環境と異なり、全てのツールが有効なのはなぜ？
zenn 環境では `importNpmLock` による node_modules のビルド生成物は cache.nixos.org に存在せず、ローカルでのビルドが必要となります。
そのため、cachix や GitHub Actions Cache にキャッシュすることでビルドが短縮できていると思われます。

##### cachix-action を用いた場合のビルド
RUN nix build ログを確認すると、適宜 cachix からダウンロードしている様子が確認できます。

- [ログリンク build #16 zenn cachix-action](https://github.com/ryuryu333/nix_cache_ci_experiments/actions/runs/18754054171/job/53501163602)

<!-- cspell:disable -->
```bash
Run nix build
# ...
these 677 paths will be fetched (166.24 MiB download, 509.45 MiB unpacked):
# ...
# 公式バイナリキャッシュがある場合は、cache.nixos.org を利用
copying path '/nix/store/pnl4n8xandadzdp0jlnbkq4smkldprzs-treefmt-2.3.1' from 'https://cache.nixos.org'...
# ...
# 公式バイナリキャッシュが無い場合は、cachix を利用
copying path '/nix/store/p9ldlklrac1mj96npxjf01mysixl1mkd-zenn-cli-0.2.3.tgz' from 'https://ryuryu333.cachix.org'...
# ...
```

##### キャッシュツールを用いないビルド
zenn-cli 等の Node.js ツールは公式バイナリキャッシュを利用せず、ローカルでビルドされています。

>実は、zenn-cli だけなら Nixpkgs に存在するので、pkgs.zenn-cli とすればキャッシュが使えます。
>今回の環境では、`importNpmLock` で Node.js ツール（zenn-cli 含む）を Nix で再現しているため、キャッシュは利用できません。

- [ログリンク build #16 zenn none cachetool](https://github.com/ryuryu333/nix_cache_ci_experiments/actions/runs/18754054171/job/53501163549)

```bash
Run nix build
# ...
these 269 derivations will be built:
# ...
these 464 paths will be fetched (265.13 MiB download, 1391.91 MiB unpacked):
# ...
# 公式バイナリキャッシュがある場合は、cache.nixos.org を利用
copying path '/nix/store/pnl4n8xandadzdp0jlnbkq4smkldprzs-treefmt-2.3.1' from 'https://cache.nixos.org'...
# ...
# 公式バイナリキャッシュが無いので、ビルド
building '/nix/store/d314dc7v1hakn4g9824yyrff2gxckg0h-zenn-cli-0.2.3.tgz.drv'...
# ...
```
<!-- cspell:enable -->
:::


:::message
#### `magic-nix-cache-action` のエラーバーが大きいのはなぜ？
`RUN nix build` の実行時間に大きなバラつきがあるためです。
次項で詳しく見ていきます。
:::

### 4.2.2 build 時間

| キャッシュツール          | 時間（秒） | vs none  |
|---------------------------|----------:|-------:|
| none                      | 51.50     | -   |
| cachix-action             | 21.25      | 41%    |
| cache-nix-action          | 9.00      | 17%    |
| magic-nix-cache-action    | 38.50      | 75%   |

>全て 2 回目のビルド＝キャッシュを利用した状態でのビルド時間

![zenn build 時間一覧](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-27_3.webp)

::::message
#### `magic-nix-cache-action` のエラーバーが大きいのはなぜ？
確証はありませんが、GitHub Actions Cache API の応答速度の影響かもしれません。

:::details 詳細を見たい方用
以下は今回の検証対象の各 job における build 時間です。

| build No.          | 時間（秒） |
|:------:|:----------:|
| #16   | 54      |
| #22   | 24      |
| #24   | 53      |
| #28   | 23      |

[リンク build #22](https://github.com/ryuryu333/nix_cache_ci_experiments/actions/runs/18777913273)

[リンク build #24](https://github.com/ryuryu333/nix_cache_ci_experiments/actions/runs/18778547830)

`#22` と `#24` を比較してみましたが、`RUN nix build` のログ内容に差はありませんでした。
つまり、実行している処理は同じであるにも関わらず、処理時間が異なるという事です。

`RUN nix build` における主な処理はパッケージのダウンロードです。
`magic-nix-cache-action` では公式バイナリキャッシュ（`cache.nixos.org`）か GitHub Actions Cache からダウンロードします。
他のキャッシュツールでも公式バイナリキャッシュを利用していますが、build 時間のバラツキは大きくありません。
ですので、消去法的に GitHub Actions Cache からのダウンロードがネックであると思われます。

もしかしたら、GitHub Actions Cache に保存しているファイル数が多い（1000 以上）のも影響しているかもしれません。
:::
::::

:::message
#### どのキャッシュツールが marp 環境においてベストか？
`cache-nix-action` がベスト。
次点は `cachix-action`。

`magic-nix-cache-action` は job が早くなる可能性はありますが、他ツールに及びません。
膨大な量のキャッシュファイルが生成される、タイミングによってはビルドが遅くなる、といったデメリットがあります。
:::


### 4.2.3 job 時間（キャッシュ生成時 vs キャッシュ利用時）

| キャッシュツール          | キャッシュ生成時<br>job 時間（秒） | vs none  | キャッシュ利用時<br>job 時間（秒） | vs none  |
|---------------------------|----------:|-------:|----------:|-------:|
| none                      | 66.25      | -   | 66.25      | -   |
| cachix-action             | 99.00      | 149%    | 40.50      | 61%   |
| cache-nix-action          | 108.25      | 163%    | 29.75      | 45%   |
| magic-nix-cache-action    | 377.00      | 569%   | 66.50      | 100%  |

![zenn job 時間一覧](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-27_1.webp)

:::message
#### キャッシュ生成速度に差はあるのか？
marp 環境と同じ傾向となりました。

`magic-nix-cache-action` が突出してキャッシュ生成時間が長くなりました。

他のツールにおいては、大きな差異は確認されませんでした。
:::


### 4.3 キャッシュ容量
おまけの検証として、キャッシュ容量を確認しました。

下記は marp、zenn 両方の環境をビルドした後、各ツールが生成したキャッシュとなります。

| キャッシュツール             | 保存場所                 | ファイル数 | 容量（MB） |
|------------------------------|--------------------------|------------:|------------:|
| cachix-action                | cachix                   | 287          | 69          |
| cache-nix-action             | GitHub Actions Cache     | 2          | 1130         |
| magic-nix-cache-action       | GitHub Actions Cache     | 2202          | 1177        |



:::details 詳細データを見たい人用
キャッシュを全て削除した後、build #33 を実行後、キャッシュを削除していない状態で確認しました。

```bash
# build #33 を実行後、キャッシュを削除していない状態で実行
$ gh api repos/ryuryu333/nix_cache_ci_experiments/actions/cache/usage
{
  "full_name": "ryuryu333/nix_cache_ci_experiments",
  "active_caches_size_in_bytes": 2307766854,
  "active_caches_count": 2202
}
```

![zenn GitHub Actions Cache 容量](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-31_4.webp)

![zenn cachix Cache 容量](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-31_1.webp)

![zenn cachix Cache 容量 個別](/images/1a2eeca4775e56/1a2eeca4775e56-2025-10-31_2.webp)

:::


