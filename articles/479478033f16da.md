---
title: "VBA でスタックトレース機能付きのロガーアドインを作成しました - 2. アドイン導入・使用方法 -"
emoji: "🌿"
type: "tech" # tech: 技術記事 / idea: アイデア
topics: []
published: false
---

# はじめに
プログラミングでデバッグを行う際、スタックトレース（関数の呼び出し履歴）は非常に重要な情報です。

エラーが起きたときに**どの関数からどの関数を経由してここに至ったのか**が分かれば、原因を突き止めやすくなります。

しかし、**VBAでは「コード実行中」にスタックトレースを取得する標準的な機能がありません**。

そこで、**VBA でスタックトレースを含むログ出力を簡単に行える**ロガーアドインを作成しました。

```:ロガーのデモ
[2025-05-22 20:24:35.891][Trace] [MyModule.MainProc] >> Enter MainProc
[2025-05-22 20:24:35.898][INFO] [MyModule.MainProc] 処理1
[2025-05-22 20:24:35.906][Trace] [MyModule.SubProc < MyModule.MainProc] >> Enter SubProc
[2025-05-22 20:24:35.922][INFO] [MyModule.SubProc < MyModule.MainProc] 処理2
[2025-05-22 20:24:35.938][Trace] [MyModule.SubProc < MyModule.MainProc] << Exit  SubProc
[2025-05-22 20:24:35.938][Trace] [MyModule.MainProc] << Exit  MainProc
```

![エクセルシートに出力されたスタックトレース付きログの例。呼び出し階層がインデントで表現されている。エクセルの機能でフィルター処理されている。](/images/5f91a2fd269ffd/5f91a2fd269ffd-2025-5-20_2.webp)

## シリーズ構成（全4回）
作成したロガーについて、4 つの記事に分けて解説を進めていきます。

| 記事 | リンク |
|------|----------|
| 1. 既存手法の紹介・設計方針 | [記事 1](https://zenn.dev/trifolium/articles/5f91a2fd269ffd) |
| 2. アドイン導入・使用方法<br>**（本記事）** | [記事 2](https://zenn.dev/trifolium/articles/479478033f16da) |
| 3. 活用例 | [記事 3](https://zenn.dev/trifolium/articles/bab9538507644c) |
| 4. 内部アーキテクチャ・設計思想 | [記事 4](https://zenn.dev/trifolium/articles/939e83c4fcc9ed) |

## 本記事について
**リファレンス的な構成**としており、本ロガーの**導入方法から基本的な使用方法**を解説します。

より実践的な活用方法については、次回の記事（活用例）で解説する予定です。

### OSS 情報
- **リポジトリ**

https://github.com/ryuryu333/vba_stack_trace_logger

- **ライセンス**
    - MIT

- **対応環境**
    - OS：Windows
    - 対応アプリ：Excel
    - VBA バージョン：7.x


# 対象読者
- **VBA 中級者**
    - VBA の基本文法を理解している
    - プロシージャ（Sub、Function）を使用したことがある
    - 標準的なログ出力機能である `Debug.Print` を使用したことがある


# アドインのダウンロード・導入
## ファイルのダウンロード
ロガーアドインの本体となるファイル（.xlam 形式）をダウンロードし、PCに保存します。

### 1. 下記サイトへアクセスします。

https://github.com/ryuryu333/vba_stack_trace_logger

### 2. リリースページ（通常「Releases」と表示されています）から最新版のアドインファイルをダウンロードします。 （ファイル名：`VbaStackTraceLogger.xlam`）

### 3. ダウンロードしたアドインファイルを任意のフォルダに保存します。

:::message
**重要：アドインファイルの保存場所について**

この後、VBA プロジェクトからこの保存場所にあるアドインファイルを参照します。

参照設定した後にアドインファイルの**保存場所を変更**したり、**ファイルを削除**したりすると、**再度参照設定が必要**になります。

**`C:\Users\YourName\Documents\vba_addins\` のように、アドイン専用のフォルダを作成し、そこに保存することを強くおすすめします。**
:::


## VBA プロジェクトからの参照設定
ロガーを利用したい Excel ファイル（`.xlsm` などマクロ有効ブック）を開き、VBA エディタ（VBE）にてアドインへの参照設定を行います。

これにより、VBA コードからロガーの機能（関数やオブジェクト）を呼び出せるようになります。

### 1. ロガーを導入したい Excel ファイルを開き、VBE を起動します。
Excel の `開発 タブ` > `Visual Basic` をクリックします。

![Excel の開発タブと Visual Basic の場所](/images/479478033f16da/479478033f16da-2025-5-26.webp)

:::details 開発タブがリボンに表示されていない場合
Excel の `ファイル タブ` > `オプション` をクリックします。
`リボンのユーザー設定` を選択します。
画面右側のリストにある `開発` のチェックボックスをオンにし、`OK` をクリックします。

![リボンのユーザー設定画面](/images/5f91a2fd269ffd/5f91a2fd269ffd-2025-5-22.webp)
:::

### 2. VBE のメニューから「参照設定」ダイアログを開きます。
`ツール` > `参照設定` をクリックします。

![VBE の参照設定の場所](/images/5f91a2fd269ffd/5f91a2fd269ffd-2025-5-22_1.webp)

### 3. 「参照設定」ダイアログで、ダウンロードしたアドインファイルを参照します。
`参照(B)` ボタンをクリックします。

「ファイル参照」ダイアログが開きます。
右下のファイル種類のドロップダウンリストで、デフォルトの`タイプライブラリ (*.olb;*.tlb;*.dll)` から `Microsoft Excel Files (*.xlsm;*.xlam;*.xls;*.xla)` を選択します。

その後、前の手順でアドインファイルを保存したフォルダに移動し、アドインファイル（`VbaStackTraceLogger.xlam`）を選択して `開く` ボタンをクリックします。

![参照画面とファイル参照画面におけるクリックする位置](/images/5f91a2fd269ffd/5f91a2fd269ffd-2025-5-22_2.webp)

### 4. 参照設定が正しく行われたことを確認します。
「参照設定」ダイアログの「参照可能なライブラリファイル(A)」のリスト内に、アドインのプロジェクト名（`VbaStackTraceLogger`）が表示され、その左側のチェックボックスがオンになっていることを確認してください。

問題なければ `OK` ボタンをクリックしてダイアログを閉じます。

![アドイン登録後の参照設定画面](/images/5f91a2fd269ffd/5f91a2fd269ffd-2025-5-22_3.webp)


## 導入確認
ここまでの手順で、VBA プロジェクトからロガーアドインの機能を利用する準備が整いました。 

実際にロガーが利用可能か、簡単なコードを実行して確認します。

### 1. 新しいモジュールを作成します。
VBE のメニューから `挿入` > `標準モジュール` をクリックします。

![標準モジュール作成のボタンの場所](/images/479478033f16da/479478033f16da-2025-5-26_1.webp)

### 2. 挿入された標準モジュールに、以下の確認用コードを記述します。

```vba
Sub CheckLogger()
    ' === ロガーの初期化 ===
    Dim config As LoggerConfigStruct
    With config
        .IsIncludeCallerInfo = False
        .IsWriteToImmediate = True
        .IsWriteToExcelSheet = False
        Set .OutputExcelSheet = Nothing
    End With
    NewMyLogger.Initialize config
    
    ' === ログの出力 ===
    myLogger.Log "これはロガーによるログ出力です"
    
    ' === ロガーの終了処理 ===
    myLogger.Terminate
End Sub
```

### 3. 確認用コードを実行します。
VBE のメニューから `実行` > `Sub/ユーザーフォームの実行` をクリックするか、F5 キーを押します。

下画像のように「マクロ」ダイアログが表示された場合は、`CheckLogger` を選択して`実行`をクリックします。

![マクロダイアログの画面](/images/479478033f16da/479478033f16da-2025-5-26_2.webp)

:::details 「マクロ」ダイアログが表示されない場合もあります
カーソルが特定のプロシージャ内（この例では `Sub CheckLogger()` から` End Sub` の間）にある状態で実行を指示すると、そのプロシージャが直接実行され、「マクロ」ダイアログは表示されません。
:::

### 4. ログ出力を確認します。
イミディエイトウィンドウを確認し、ログが以下の様に出力されているか確認します。

```:実行結果
ロガーの初期化を開始します
イミディエイトウィンドウ用のロガーを初期化しました
ロガーの初期化が完了しました
[2025-05-26 22:29:14.859][INFO] これはロガーによるログ出力です
```

:::details VBE でイミディエイトウィンドウが表示されていない場合
VBE のメニューから `表示` > `イミディエイト ウィンドウ（Ctrl + G）` をクリックします。

![イミディエイトウィンドウを表示するボタンの場所](/images/479478033f16da/479478033f16da-2025-5-26_3.webp)
:::


# ロガーの使用方法
## ロガーの初期化
ロガーアドインの機能を利用する前に、まずロガーを初期化する必要があります。
**ロガーアドインの機能を利用する前にロガーを初期化する必要があります。**

初期化処理では、**ログの出力先**や**スタックトレース情報の出力有無**といった、ロガーの動作をカスタマイズできます。

記述が長くなるので、まずは要素ごとに解説し、**最後に初期化処理のコード全体を提示**します。

### 1. `LoggerConfigStruct` というユーザー定義型の変数を宣言します。
ロガーの設定情報を格納するための専用の変数 `config` を宣言します。

```vba
Dim config As LoggerConfigStruct
```

:::details  `LoggerConfigStruct` ユーザー定義型とは？
ユーザー定義型とは、複数の異なる型の変数を 1 つのまとまりとして扱えるようにする VBA の機能です。

`LoggerConfigStruct` は本ロガーで定義しているユーザー定義型で、ロガーの各種設定値をこの中に集約して管理します。

https://learn.microsoft.com/ja-jp/office/vba/language/reference/user-interface-help/type-statement
:::


### 2. `LoggerConfigStruct` の各メンバー（設定項目）に値を設定します。

```vba
With config
    ' スタックトレース情報を含むか（True/Flase）
    .IsIncludeCallerInfo = True
    ' イミディエイトウインドウへログを出力するか（True/Flase）
    .IsWriteToImmediate = True
    ' エクセルシートへログを出力するか（True/Flase）
    .IsWriteToExcelSheet = True
    ' ログを書き込みエクセルシートを指定（Worksheet オブジェクト）
    Set .OutputExcelSheet = ActiveSheet
End With
```

:::details エクセルシート（Worksheet ブジェクト）の設定例

```vba
' 現在アクティブな（表示されている）Excel シート
ActiveSheet

' マクロが記述されている Excel ファイル（ThisWorkbook）内の
' "SheetName" という名前のシート
' ※該当する名前のシートが存在しない場合はエラーになるため注意
ThisWorkbook.Sheets("SheetName")
```

:::

### 3. `NewMyLogger.Initialize config` プロシージャを呼び出し、設定を反映してロガーを初期化します。

```vba
NewMyLogger.Initialize config
```

### コード全体

```vba
Sub InitializeLoggerExample()
    ' === ロガーの初期化 ===
    ' 1. ロガーの設定情報を格納するための変数を宣言
    Dim config As LoggerConfigStruct

    ' 2. 各設定項目に値を代入
    With config
        ' スタックトレース情報を含むか（True/Flase）
        .IsIncludeCallerInfo = True
        ' イミディエイトウインドウへログを出力するか（True/Flase）
        .IsWriteToImmediate = True
        ' エクセルシートへログを出力するか（True/Flase）
        .IsWriteToExcelSheet = True
        ' ログを書き込みエクセルシートを指定（Worksheet オブジェクト）
        Set .OutputExcelSheet = ActiveSheet
    End With

    ' 3. 設定を反映してロガーを初期化
    NewMyLogger.Initialize config
End Sub
```

:::message
**重要：初期化のタイミングと回数**

**ロガーの各種機能を利用する前に、必ずこの初期化処理を実行してください。** 
（初期化せずにログ出力やスタックトレース機能を使用しようとすると、エラーが発生しポップアップが表示されます。）

一度初期化を行えば、その設定は VBA の実行が終了するまで有効です。
プロジェクト内の他の標準モジュールやクラスモジュールからロガーを使用する場合でも、再度初期化を行う必要はありません。

初期化後は、後続する処理ならばどの標準モジュール、クラスモジュールでロガーを使用する場合でも、再度の初期化は不要です。

意図しない動作の変更や処理のオーバーヘッドを避けるため、VBAコードの実行中に NewMyLogger.Initialize を複数回呼び出すことは非推奨です。 通常、アプリケーション起動時や一連の処理の開始時に一度だけ呼び出します。

よって、**VBA コード実行中にロガーの初期化を 2 回以上呼び出すことは非推奨**です。
:::


## スタックトレース機能
（関数の入退場処理）

## ログの出力
（各ログレベル、出力先設定）

## ロガーの終了処理
（リソース解放、注意点）

# 完全なサンプルコード
（全機能を組み合わせた実用例）